# NodeJS CouchDB Demo

## Build Setup

``` bash
# Install Dependencies
npm install express body-parser ejs node-couchdb --save

# Install Nodemon
npm install -g nodemon

# Run Application
nodemon
```
